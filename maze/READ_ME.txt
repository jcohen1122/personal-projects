This project features a maze generator, where the user inputs a dimension that determines the size of the maze.

The project also features a maze solver, which shows the path to the finish cell of the maze, with adjustable speeds.

To get started, use the Driver
